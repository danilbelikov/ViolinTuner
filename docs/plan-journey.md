# План: путешествие по залам мира за ноты в строе (spec 3.23, 5.17, этапы 52–55)

Хэндофф — `docs/design/project/journey/project/Путешествие.dc.html` (кадры 26a–26l; сцены — функции `HOME`, `CREMONA`, `MILAN`, `VIENNA`, `VIENNA_INT`, `LONDON`, `SYDNEY`, `AUSTRIA` в его JS, палитры `PAL`, карта `MAP` / `LAND` / `LBL`, силуэты штампов, секция `dev`). Бриф — `docs/design/brief-journey.md`. Спека сверена с ним в версии 0.40.

## Что ответил дизайн и что берём

| Вопрос / решение макета | Что делаем |
|---|---|
| Валюта — «такт»: нота в строе = 1, минута занятия = 2; знак — нота со штилем-дорогой (26i) | Берём; знак — `TaktIcon` на `Canvas` |
| Маршрут линейный, 16 остановок, цены 300…15 000; в макете сумма названа 81 700 | Берём цены; настоящая сумма — **81 900** (тест считает её сам) |
| Экран путешествия 26a: открытка 240 dp, таблетка «остановка N из 16», карточка пути, залитая «В путь · N» / тональная «не хватает N», лента открыток, «Карта», «Паспорт» | Берём |
| Детализация открыток — уровень «как надо», ~50 слоёв (26b2); день — та же геометрия с палитрой `day` (26b4); второй вид Вены — зал (26b5) | Берём; сцены не переписаны руками, а **выгружены** из JS хэндоффа (см. ниже) |
| Нарисовано шесть мест (Дом, Кремона, Милан, Вена ×2, Лондон, Сидней) и запасная «альпийская» открытка | Берём все; альпийскую ставим **Зальцбургу** (горы, озеро, церковь с луковкой — макет держал её «страховкой») |
| Остальные остановки в макете — только силуэты штампов | Открытка-эскиз: силуэт на закатном небе. Прага, Лейпциг, Берлин, Амстердам проезжаются с эскизом; остановки после Амстердама закрыты («дорога ещё рисуется») |
| Экран остановки 26c: чипы «вечер · день · внутри», «Дополнения» с ценами 200 / 400 / 150 | Берём; чипы показываются только у открытого (запертый чип — дверь перед носом), вид — парой «снаружи · внутри» |
| Вступление 26d: «Дорога начинается дома», «Собрать футляр» | Берём |
| Карта 26e: схема 412 × 700, Катмулл — Ром, три стиля линии, подписи по мере приближения, щипок 1–2,5× | Берём; карта вписана целиком, сдвиг — только на величину увеличения |
| Дорога 26f: карта ×1,8 на перегоне, транспорт едет по линии, 2 / 2,5 с; прибытие 600 мс; штамп 350 мс | Берём; транспорт — три простых силуэта (поезд, корабль, самолёт) |
| Паспорт 26g: сетка штампов, четыре краски, пунктир у будущих, «скоро» | Берём; сетка адаптивная (от 104 dp), а не жёстко три колонки |
| Окно на «Занятиях» 26h1: открытка 160 dp под полосой уровня | Берём; в landscape — компактная (96 dp) в левой колонке **под** сводкой, чтобы кнопка занятия осталась на виду |
| Лист «Занятие сохранено · +340 тактов · 264 ноты в строе + 76 за время» после сохранения (26h3) | **Не делаем**: лист итога в приложении — *до* сохранения, листа «после» нет и заводить его ради числа — лишнее касание. Вместо него — таблетка «+340 тактов» на карточке на 3 с (она же в 26h2) |
| Название и значок приложения «Music Journey» (26l) | **Не в этот раз**: переименование приложения — отдельное решение владельца (spec 7) |
| Движение в открытках (дым, мерцание огней — `anim: true`) | **Не делаем**: на «Занятиях» уже блик, таймер и огонёк (3.16, 3.18); открытка неподвижна |
| Множитель тактов за строгий допуск | **Не делаем**: путешествие не должно подталкивать менять настройки |

## Решения, которых нет ни в спеке, ни в макете

- **Сцены выгружаются, а не переносятся руками.** Сцены хэндоффа — JS-функции, возвращающие слои (`path`, заливка-токен, глубина). Скрипт на node исполняет их и пишет `assets/journey/<сцена>.<eve|day>.scene`: строка на слой, поля через табуляцию. `SceneParser` читает, `ScenePalette` разрешает токены (общая палитра времени суток + палитра места), `SceneCanvas` рисует. Сцену меняют в хэндоффе и выгружают заново — как иконки. `SceneTest` проверяет, что каждый токен каждой выгруженной сцены разрешается и что у каждого вида маршрута есть оба файла.
- **Такты считает конвейер записи, а не экран**: `NoteCounter` стоит в `TakePipeline.run` рядом с движком, на его потоке, время — `PitchFrame.tMs`. Поэтому ноты считаются и на Live, и при записи дубля одним кодом. Без идущего занятия счётчик сбрасывается вхолостую.
- **Нота «в строе» — большинство свежих показаний в зелёной зоне движка** (с гистерезисом), а не среднее отклонение: так нота, которую видели зелёной, и засчитана.
- **Недосчитанные ноты — в DataStore, привязаны к началу занятия** (`practice_notes_for / played / in_tune`), дописываются раз в 10 с и в `onCompletion` под `NonCancellable`: смерть процесса теряет не больше 10 с игры; счёт чужого занятия не примешивается.
- **Начисляет `PracticeFinisher.save`** — единственная дверь сохранения занятия (лист итога и диалог забытого занятия): такты не начисляются дважды по той же причине, по которой занятие не сохраняется дважды. «Изменить время» идёт мимо `save` и тактов не даёт — намеренно.
- **Баланс не хранится**: заработки − цены прибытий − цены дополнений. Цена пишется в строку покупки. Отправление и покупка — транзакции DAO «проверить баланс → вставить».
- **Дорога — фаза экрана, а не состояние путешествия.** Остановка оплачена и достигнута до того, как поезд тронулся; `JourneyPhase` (Road → Arrival → Stamp) живёт во view model. Смерть процесса в дороге: остановка уже открыта, штамп в паспорте уже стоит — пропущено только кино.
- **Пока экран покоя гаснет, он показывает прежнюю остановку**: состояние уже в новом городе, и без этого за 300 мс кроссфейда мелькал бы спойлер.
- **Карта, паспорт и главный экран — три вида одного `JourneyViewModel`** (`JourneyView`), у каждого маршрута свой экземпляр; экран остановки — `StopViewModel`.
- **Окно на «Занятиях» — отдельный поток `PracticeViewModel.journeyWindow` и слот `journeyCard` у `PracticeScreen`**, не поле `PracticeState`: редьюсер «Занятий» и его тесты не тронуты. «+N тактов» показывается только для заработка, пришедшего при живом экране: первое прочитанное значение — история, не новость.
- **Сувенир** — общая бумажная наклейка с нотой рядом со штампом; свои сувениры у остановок — позже (spec 7).

## После хэндоффа: четыре открытки (spec 0.41)

- Прага, Лейпциг, Берлин и Амстердам дорисованы без нового макета — теми же примитивами и правилами хэндоффа (три плана, две грани, окна и колонны правилом, два фонаря, дерево, герой): `tools/journey/extra-scenes.js`, палитры мест — `EXTRA_LOCPAL` там же и в `ScenePalette.locations`. Что нарисовано: Рудольфинум с балюстрадой и статуями, Град со шпилями вдали; церковь Св. Фомы — крутая черепичная крыша, башня с медным шлемом, Бах на постаменте; Филармония — три охристых «шатра» без параллельных линий над стеклянным фойе; Консертгебау — кирпич, храмовый портик, золотая лира, канал с лодкой, щипцовые дома вдали.
- **Генератор переехал в репозиторий** — `node tools/journey/export.js [папка предпросмотра]`: сам вырезает из `Путешествие.dc.html` помощники, сцены и силуэты (по якорям `const hex2 =` … `let gid`, `const SCENES =`, `const SIL =`; пропали якоря — скрипт падает с объяснением), добавляет `extra-scenes.js`, пишет `.scene` и `JourneySilhouettes.kt`; с папкой — ещё и SVG каждой сцены, нарисованный как в хэндоффе (на macOS в PNG — `qlmanage -t -s 824 -o . файл.svg`). Прежние сцены он воспроизводит байт в байт.
- Вторых видов (залы изнутри) у новых остановок нет — у них два дополнения: день и сувенир.
- Проверено: предпросмотр вечера и дня всех четырёх на хосте, в приложении на эмуляторе — главный экран и лента открыток (прибытия подложены строками в `journey_arrivals`). Не видел ни дизайнер, ни владелец до коммита.

## После хэндоффа: живая открытка и полный экран (spec 0.42)

Макета нет — владелец попросил словами; образцы — пюпитр и полноэкранное видео.

- **Движение — по токену заливки, а не по разметке сцен**: `SceneMotion` (чистый, с тестами) — `window` / `windowLit` / `chandelier` мерцают (период и фаза от номера слоя — из `flick` хэндоффа), `GLOW` и тёплые ореолы дышат, `waterLit` дрейфует. Сцены и формат `.scene` не менялись. Отражения окон в канале Амстердама поэтому перекрашены в литерал — иначе уплывали бы вместе с бликами.
- **Параллакс без датчиков**: `SceneCamera` (чистый, с тестами) — масштаб «покрыть», зажим сдвига по тому, что у картинки есть за рамкой, сдвиг плана по глубине (0,6 · 0,85 · 1). Тест перебирает масштабы и оба края: ни один план не показывает свой край. Наклон телефона — позже (spec 7).
- Время — `rememberSceneSeconds`: шагает кадрами не чаще 30 в секунду, читается в фазе отрисовки (`Postcard(seconds, camera)` — открытка перерисовывается, не перекомпоновывается); при «убрать анимации» и вне экрана его нет.
- **Полный экран — состояние экрана остановки** (`StopState.fullscreen`, как у видео), а не маршрут: выбор «день / внутри» общий, «назад» сворачивает (`StopIntent.BackClicked` + `BackHandler`).
- Где открытка живёт: экран остановки и полный экран. Карточка на «Занятиях», главный экран, лента, прибытие — неподвижны намеренно.
- Проверено на эмуляторе: вход тапом, панорама свайпом, параллакс, панель уходит сама, два кадра с паузой различаются (движение идёт). Не проверено: движение глазами, щипок (эмулятор его не даёт), landscape, «убрать анимации», расход батареи на телефоне.

## Поправки владельца к живой открытке (spec 0.43)

- «Пока не вижу анимаций» — анимации не были выключены, они были слишком тихими (окна −14 % за 4–6 с). Амплитуды подняты (окна −32 %, ореолы −40 % за 3,5 с, вода ±14 за 6 с) и добавлено то, что видно сразу: звёзды вечером и облака днём — не слои сцен, а правило в отрисовке (`SceneMotion.star / cloud`, детерминированный хеш: небо каждый раз то же), рисуются сразу над слоем `SKY`, только у сцен под открытым небом.
- «Анимировать везде» — открытка живёт в карточке на «Занятиях», на экране путешествия, во вступлении, прибытии, на остановке и в полном экране; миниатюры ленты неподвижны. Это отменяет моё решение из 0.42 держать «Занятия» тихими.
- «Отдаление до всей открытки» — `SceneCamera.wholeZoom`, двойной тап по кругу (`nextZoom`), под открытым небом открытка встаёт на нижний край (`top`). Заодно найдено: плоское небо над рамкой (`sky`) смешивалось с воздухом и не совпадало с верхом градиента — токен `sky` из воздушной перспективы исключён.
- Проверено на эмуляторе: вся открытка по ширине двойным тапом, звёзды; запись экрана «Занятий» за 6 с — кадры идут. Глазами на телефоне — нет; если окажется слишком живо, числа — в `SceneMotion`.

## Остальные шесть открыток — маршрут открыт целиком (spec 0.44)

- Париж, Санкт-Петербург, Москва, Нью-Йорк, Буэнос-Айрес и Токио дорисованы в `tools/journey/extra-scenes.js`; новые общие приёмы там же — `GROUND`, `palm`, `blossom`. Свои узнаваемые детали: золочёные группы и медный купол Гарнье; купол Исаакия и игла Адмиралтейства вдали; сидящий Чайковский с поднятой рукой; маркиза с лампочками и жёлтое такси; пальмы; Токийская башня, сакура и освещённый стеклянный вход.
- **Все остановки теперь `available`** — закрыты они были только потому, что не было картинок. Флаг остался для будущих городов; после Сиднея экран говорит «Мировое турне пройдено».
- **Палитры дорисованных мест больше не копируются руками**: экспортёр пишет `ExtraScenePalettes.kt` из `EXTRA_LOCPAL`, `ScenePalette.token` смотрит и туда.
- Факты о пяти местах написаны заново (у Лондона, Нью-Йорка и Сиднея были из хэндоффа): Шагал в Гарнье (1964), Седьмая Шостаковича в блокаду (август 1942), конкурс Чайковского и 1901 год, «Аида» 1908 года в Колоне, зал-«виноградник» 1986 года.
- Проверено: предпросмотр вечера и дня на хосте; в приложении — по одной открытке на эмуляторе. Не видел ни дизайнер, ни музыкант из этих городов; перегоны кораблём и самолётом на экране по-прежнему не проезжались.

## Этапы

52. Домен и хранение — `core/domain/journey`, `NoteCounter` в `TakePipeline`, начисление в `PracticeFinisher`, Room v9 (`journey_earnings`, `journey_arrivals`, `journey_extras`), `DataStorePracticeNotesStore`; тесты домена, конвейера, миграции.
53. Открытки — выгрузка сцен, `feature/journey/art` (`Scene`, `SceneCanvas`, `JourneySilhouettes`), `SceneTest`.
54. Экраны — `JourneyScreen` (вступление, покой, дорога, прибытие, штамп), `JourneyMap` + `JourneyMapMath`, `StopScreen`, `PassportScreen`, `MapScreen`, маршруты `journey`, `journeyMap`, `journeyPassport`, `journeyStop/{stopId}`; `JourneyViewModelTest`, `JourneyMapMathTest`.
55. Окно на «Занятиях» — `JourneyWindowCard`, `journeyWindow`, `PracticeIntent.JourneyClicked` / `PracticeEffect.OpenJourney`; тест в `PracticeViewModelTest`.

## Как вышло

- Проверено на эмуляторе на сборке `-PfakePitch=true`: настоящее занятие на Live дало строку заработка (4 ноты сыграно, 0 в строе — фейковый сценарий держит одну ноту, которая уплывает; 2 такта за минуту); дальше такты подложены строкой в `journey_earnings`. Пройдено: вступление → Дом → Кремона → Милан → Зальцбург → Вена, дорога, прибытие, штамп, экран остановки с покупкой второго вида, паспорт, карта, карточка на «Занятиях».
- Найдено на эмуляторе и поправлено: спойлер следующего города в кроссфейде; карта, обрезанная по высоте (теперь вписана целиком); дата, наезжавшая на название в штампе; «скоро новые горо…» в паспорте → «скоро»; один чип «внутри» → пара «снаружи · внутри».
- Не проверено: телефон и живой инструмент (сколько тактов даёт настоящее занятие — главное число фичи, цены стартовые); landscape (эмулятор не повернулся); таблетка «+N тактов» глазами (есть unit-тест); дорога кораблём и самолётом (перегоны закрыты); «убрать анимации»; TalkBack; открытки днём, кроме как тестом разбора.
